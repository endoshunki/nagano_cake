<h5>商品詳細</h5>
<% if @item.image.attached? %>
    <%= image_tag @item.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>
<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>商品名</td>
      <td><%= @item.name %></td>
    </tr>
    <tr>
      <td>商品説明</td>
      <td><%= @item.introduction %></td>
    </tr>
    <tr>
      <td>税込価格<br>
      (税抜価格)</td>
      <td><%= @item.add_tax_price.to_s(:dalimited) %><br>
      <%= @item.price %></td>
    </tr>
  </tbody>
</table>
<%= link_to "編集する", edit_admin_item_path %>